# Privora Panel 5.0.0-dev.6.60 — Live Domain Reconciliation & AutoSSL HTTP-01 Repair

Built cumulatively from the installed 5.0.0-dev.6.59 baseline.

## Repairs
- Correct production `dig` ordering (`name type`) for local authoritative NS, delegation, and glue checks.
- Add exact localhost authoritative-SOA fallback for known hosted domains that BIND serves but whose zone file is not discoverable.
- Add effective `nginx -T` exact `server_name` fallback for active legacy/imported vhosts that the structured parser misses.
- Add `location ^~ /.well-known/acme-challenge/` to new HTTP vhosts before the hidden-file deny rule.
- Reconcile that ACME exception into existing managed/legacy Nginx vhosts, including enabled symlink targets.
- AutoSSL now performs a public HTTP-01 token probe before Certbot and separates DNS pending, HTTP blocked, and issuance failure states.
- Certbot failures surface useful validation details from the ACME log.
- PCC keeps `Privora Command Center` on one line above the logo/status row.

## Certification
- Complete local `scripts/verify.sh` passed end-to-end after the final reconciler change.
- Exact 6.59 Update Center verifier accepted the finished signed package.
- Package: `privora-panel-5.0.0-dev.6.60-update.zip`
- SHA-256: `9bbc59b1d9a78b8c6bcede4d1405f5f832720676dbd18855eab764df90c158b0`
- Minimum installed version: `5.0.0-dev.6.59`

Live deployment remains gated on the user installing the signed Update Center package and confirming VPS behavior.
